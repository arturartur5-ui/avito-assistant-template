# Зависимостей нет — только стандартная библиотека. Файл нужен, чтобы
# версия Python была записана одним местом: синтаксис кода требует 3.10+.
[project]
name = "avito-assistant"
version = "0.1.0"
description = "ИИ-помощник для чатов Авито голосом конкретного специалиста — шаблон"
requires-python = ">=3.10"
dependencies = []
